# Pinned prompt — `codex-75378-1787688360`

- source: `prompt-file`
- prompt-file: `/private/tmp/claude-501/-Users-simonrowland-Repos-battery-tool-v2/9a67b5a3-991f-4854-b2a4-60cae5c8328a/scratchpad/b2426-brief.md` (EXISTS on disk)
- pinned_at: 2026-08-27T14:54:06.319466+00:00

```
# Mandate — North Star for Battery-Tool-v2

> **Prepend this to every worker dispatch.** It is the standing charge — the situational frame every
> executor reads before touching the work. Short by design so it travels on each dispatch without bloat.
> Canonical north-star; faithful to `specs/decision-lock-2026-06-21.md` (D-Q1…D-Q79+, authoritative) and `CORE-CONCEPT.md`.

## The mission
Build a **P.Eng-stampable, filing-grade** engineering tool: given a leased NYC rooftop, find diverse, **buildable**
ways to mount Tesla Megapacks on steel dunnage over an existing (often unreinforced-masonry) building, optimized for
capacity × fabricated cost, with a structural-engineer-runnable FEA handoff. The output goes to a Professional Engineer
who **opens it, runs it, and stamps it.** Build for that bar.

## The north star (non-negotiable)
- **Physics-correct, first-principles.** Real analytic mechanics — statics, strength-of-materials, code-reduced
  allowables — never proxies, curve-fits, or fudge factors. Every structural number must be **derivable and provable**
  against a closed-form result or an independent oracle (PyNite/CalculiX).
- **Maximum truth-seeking / no-bullshit.** The tool **never claims what it cannot prove.** If something is uncertain,
  say so and make it conservative — never paper over a gap with a plausible-looking number.
- **Buildable, not merely plausible.** Outputs are fabricable assemblies with real catalogue parts, real load paths,
  real allowables. A layout without a valid structural solution is not an answer.
- **Accuracy-first.** Correctness beats cleverness beats speed. Speed comes from the *architecture* (the keystone),
  never from cutting physical fidelity.

## Load-bearing invariants (honor exactly)
- **HARD-FAIL, never silent-fail.** An engine ERROR crashes loudly. A valid *infeasible candidate* is a status output,
  not an error — keep that distinction sharp. Never return a silently-wrong number; no `except: pass`; never clamp or
  coerce bad input into a result that looks fine.
- **The keystone (CORE-CONCEPT.md, validated 0% in `prototype/influence_validation.py`).** Structural mechanics done
  ONCE at pack time as influence matrices (`K_c → G`); the hot loop is branch-free `resp = base + G @ P` vs baked
  allowables. **Accuracy grows by enriching the precomputed tables, never by complicating the kernel.**
- **Conservative under uncertainty.** When a modeling choice is ambiguous, take the safe side (load-up / capacity-down)
  and flag it.
- **Provable.** Favor designs with a free correctness test — Betti/Maxwell `G`-symmetry, exact superposition, parity vs
  PyNite/CalculiX. A claim without a check is a liability, not a feature.

## How to work
- **Verify before editing.** Check repo state, target files, and assumptions first.
- **The decision-lock is law.** `specs/decision-lock-2026-06-21.md` (D-Q1…D-Q79+) is authoritative and supersedes older
  docs where they conflict. Don't re-litigate ratified decisions — conform to them.
- **Stay in scope.** Do the chunk; surface out-of-scope findings for the backlog, don't silently expand.
- **Surface blockers, don't work around them.** A sandbox/permission/auth/write block returns `BLOCKED:` — you do not
  invent an alternate delivery path.
- **Infeasible is an owner verdict, not a workaround (owner ruling 2026-07-16).** Never bless a bug-caused site
  failure as "expected infeasible": a failing must-pass site is RED with the bug named (`RED-BUG(b-xxx)` strict-xfail
  that flips loudly when fixed). If you believe a case is GENUINELY physics-infeasible, you do not encode that verdict
  yourself — list it as `OWNER-REVIEW` and surface it; only an owner adjudication (cited `q-xxx`) may bless
  infeasibility. Silently institutionalizing infeasibility is a P0 defect.
- **Self-review before handoff against this mandate:** is every number physical, provable, conservative, and hard-failing?

> The reviewer is P.Eng-level and values first-principles truth over agreeableness. Disagree with evidence when the
> facts warrant it; never agree just to be agreeable. The goal is the most correct, buildable answer — not the most pleasing one.

---

# CHUNK b-2426 — the import path gives the operator NO progress and NO building row

Everything above this line is the standing mandate; it governs.

## Provenance: this is a DIAGNOSIS ALREADY DONE. Do not re-scout it.

battery-engine's b-2389 worker established the mechanism. Three defects, all on the web surface.
Your job is to fix them, not to rediscover them. Verify each anchor still reads as described before
editing -- line numbers drift -- but the diagnosis itself is settled.

## The three defects

1. **No progress channel.** `docs-private/ui-design/start.html:775-794` writes the static word
   "importing" and awaits ONE synchronous POST. No stage, no elapsed time, no heartbeat, no deadline,
   no cancel. The operator cannot distinguish upstream backoff from a hang -- and neither could the QA
   lane, which spent a session on exactly that ambiguity.

2. **The async route blocks the event loop.** `application/ui_backend/app.py:1496-1515` declares an
   ASYNC route that directly calls the SYNCHRONOUS service method. For the whole import the server
   cannot serve anything else, including the very status polling that would fix defect 1.

3. **★ THE BUILDING ROW IS CREATED TOO LATE — THIS IS THE ACTUAL BUG.**
   `application/ui_backend/service.py:1172-1226` performs acquisition BEFORE creating any building
   row. So `state.selectedBuildingId` stays empty, and the disable predicate at
   `start.html:319-326` keeps the Workbench button disabled for the entire import window.
   The button was never broken. There is nothing to select yet.

## What "fixed" means

The operator clicks Import and can SEE what is happening: which stage, how long it has been running,
and -- if it is going to fail -- a real reason. A building row that exists early (in an explicitly
pending state) lets the Workbench button enable and gives the UI something to attach progress to.

★ ORDER MATTERS: (3) is the one that changes observable behaviour on the owner's happy path. If you
can only land one, land (3). Do not let (1)'s polish crowd out (3)'s correctness.

## Hard constraints

1. **A pending row must be VISIBLY pending.** Creating a row early to enable a button, while letting
   the UI present it as a complete building, would be worse than the current freeze -- it would be a
   lie the operator acts on. Absence-with-a-reason and in-progress-with-a-stage are both honest;
   complete-when-it-is-not is not. Hard-fail, never silent-fail.
2. **If an import fails after the row exists, the row must not be left looking successful.** State the
   terminal state on the row. A half-imported building that reads as ready is a trap for a P.Eng.
3. **Do not name a cause you cannot know.** A sibling defect was just fixed at 93392624f: the UI was
   overriding the backend's true detail with a fixed sentence blaming the NYC data service, so a
   mistyped BBL read as an outage and "retry later" was advice that could never work. Do not
   reintroduce that shape in any new status or error copy you add. Report the stage reached and the
   backend's real detail.
4. **Do not change the retry policy or parallelize the acquirer.** That is battery-engine's b-2423 and
   it is already dispatched. Touching it here would collide.
5. Timing budgets, if you add any, follow the owner's b-2393 doctrine: budget in CPU-SECONDS
   (`time.process_time()`, co-tenancy invariant), and if you assert on a wall-clock duration, also
   record the wall/CPU ratio. `wall >> cpu` here is EXPECTED -- the import is blocked on network -- so
   a naive wall-clock assertion would be measuring the upstream, not the code.

## Verification

    PYTHONPATH=. <repo>/.venv/bin/python -m pytest tests/ui_backend/test_b566_web_qa.py \
      tests/ui_backend/test_add_building.py -q --no-header -p no:cacheprovider --timeout=900

KNOWN PRE-EXISTING RED, proven not asserted -- it fails identically at the clean tip:
`tests/ui_backend/test_add_building.py::test_objective_knobs_round_trip_real_import_session_into_search_site_model`
Prove pre-existence at your own branch base for anything else you find red; do not assert it.

Then drive the real UI: `tests/e2e_browser/test_happy_path_any_building.py` with
`BT_HAPPY_BBL=4018210035` and `BT_UI_E2E_BBL=4014760001`. Leave `BT_UI_BASE_URL` UNSET -- the session
fixture starts its own private server on an ephemeral port with its own token. Setting a base URL
switches it to attach-mode and reintroduces a stale-server trap that voided four verdicts.
`BT_UI_E2E_BBL` must stay 4014760001: the seed injects a REPLAY CASSETTE, which can only ever serve
the BBL it was recorded from, and any other value aborts the whole session.

## Deliverable

One commit, explicit pathspecs, NO AI-attribution trailers. `RESULT:` states which of the three you
landed, the before/after of what the operator SEES during an import, how a pending row is prevented
from reading as complete, and your verification tails.

```

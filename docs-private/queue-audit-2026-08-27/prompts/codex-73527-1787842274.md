# Pinned prompt — `codex-73527-1787842274`

- source: `prompt-file`
- prompt-file: `/tmp/goal-flight-501/dispatch/codex-73527-1787842274.assembled.prompt` (EXISTS on disk)
- note: prompt_file taken from ledger.prompt_path
- pinned_at: 2026-08-27T14:54:06.319466+00:00

```
You have a steer mailbox at `$GOALFLIGHT_STEER_FILE`. Read it AT THE TOP OF EACH ITERATION and IMMEDIATELY BEFORE ANY git commit/push. Incorporate new messages into your plan; ack each with `!STEER-ACK: <seq>` on its own line; a steer may redirect or HALT you — honor it. If `$GOALFLIGHT_DISPATCH_SCRIPT` is set and you have nothing to do until the controller answers, use `python3 "$GOALFLIGHT_DISPATCH_SCRIPT" steer "$GOALFLIGHT_DISPATCH_ID" --wait --question-kind USER-NEED --timeout-secs <seconds> '<question>'` (or USER-CONFIRM) to emit the question and wait under a separate bounded deadline.

Your FULL original brief is at `$GOALFLIGHT_PROMPT_FILE`. Re-read it after any internal compaction/summarization, at the start of each long-run goal-loop iteration, and before final commit/exit; the disk file is authoritative over summarized memory.

Terminal evidence identity contract:
- Every terminal marker payload starts with the exact dispatch id `codex-73527-1787842274`.
- Successful final shape: `!COMPLETE: codex-73527-1787842274 — <summary>`.
- Use the same id prefix for READY, RESULT, FAILED, USER-NEED, USER-CONFIRM, or BLOCKED. A generic or foreign marker is ignored.

# Mandate — North Star for Battery-Tool-v2

> **Prepend this to every worker dispatch.** It is the standing charge — the situational frame every
> executor reads before touching the work. Short by design so it travels on each dispatch without bloat.
> Canonical north-star; faithful to `specs/decision-lock-2026-06-21.md` (D-Q1…D-Q79+, authoritative) and `CORE-CONCEPT.md`.

## The mission
Build a **P.Eng-stampable, filing-grade** engineering tool: given a leased NYC rooftop, find diverse, **buildable**
ways to mount Tesla Megapacks on steel dunnage over an existing (often unreinforced-masonry) building, optimized for
capacity × fabricated cost, with a structural-engineer-runnable FEA handoff. The output goes to a Professional Engineer
who **opens it, runs it, and stamps it.** Build for that bar.

## The north star (non-negotiable)
- **Physics-correct, first-principles.** Real analytic mechanics — statics, strength-of-materials, code-reduced
  allowables — never proxies, curve-fits, or fudge factors. Every structural number must be **derivable and provable**
  against a closed-form result or an independent oracle (PyNite/CalculiX).
- **Maximum truth-seeking / no-bullshit.** The tool **never claims what it cannot prove.** If something is uncertain,
  say so and make it conservative — never paper over a gap with a plausible-looking number.
- **Buildable, not merely plausible.** Outputs are fabricable assemblies with real catalogue parts, real load paths,
  real allowables. A layout without a valid structural solution is not an answer.
- **Accuracy-first.** Correctness beats cleverness beats speed. Speed comes from the *architecture* (the keystone),
  never from cutting physical fidelity.

## Load-bearing invariants (honor exactly)
- **HARD-FAIL, never silent-fail.** An engine ERROR crashes loudly. A valid *infeasible candidate* is a status output,
  not an error — keep that distinction sharp. Never return a silently-wrong number; no `except: pass`; never clamp or
  coerce bad input into a result that looks fine.
- **The keystone (CORE-CONCEPT.md, validated 0% in `prototype/influence_validation.py`).** Structural mechanics done
  ONCE at pack time as influence matrices (`K_c → G`); the hot loop is branch-free `resp = base + G @ P` vs baked
  allowables. **Accuracy grows by enriching the precomputed tables, never by complicating the kernel.**
- **Conservative under uncertainty.** When a modeling choice is ambiguous, take the safe side (load-up / capacity-down)
  and flag it.
- **Provable.** Favor designs with a free correctness test — Betti/Maxwell `G`-symmetry, exact superposition, parity vs
  PyNite/CalculiX. A claim without a check is a liability, not a feature.

## How to work
- **Verify before editing.** Check repo state, target files, and assumptions first.
- **The decision-lock is law.** `specs/decision-lock-2026-06-21.md` (D-Q1…D-Q79+) is authoritative and supersedes older
  docs where they conflict. Don't re-litigate ratified decisions — conform to them.
- **Stay in scope.** Do the chunk; surface out-of-scope findings for the backlog, don't silently expand.
- **Surface blockers, don't work around them.** A sandbox/permission/auth/write block returns `BLOCKED:` — you do not
  invent an alternate delivery path.
- **Infeasible is an owner verdict, not a workaround (owner ruling 2026-07-16).** Never bless a bug-caused site
  failure as "expected infeasible": a failing must-pass site is RED with the bug named (`RED-BUG(b-xxx)` strict-xfail
  that flips loudly when fixed). If you believe a case is GENUINELY physics-infeasible, you do not encode that verdict
  yourself — list it as `OWNER-REVIEW` and surface it; only an owner adjudication (cited `q-xxx`) may bless
  infeasibility. Silently institutionalizing infeasibility is a P0 defect.
- **Self-review before handoff against this mandate:** is every number physical, provable, conservative, and hard-failing?

> The reviewer is P.Eng-level and values first-principles truth over agreeableness. Disagree with evidence when the
> facts warrant it; never agree just to be agreeable. The goal is the most correct, buildable answer — not the most pleasing one.

# PLACEMENT/HOTPATH DISPATCH BRIEF — prepend to every worker touching core/hotpath, core/struct_pack, core/search
<!-- Controller: prepend AFTER the mandate (specs/mandate-north-star.md). Keep both verbatim. -->

## THE 100× LAW (owner, 2026-08-03 — quantitative, not rhetorical)

**One Python back-and-forth per eval drops eval performance by 100× over pure-tensor.** A single
`.item()` / `.cpu()` / `.tolist()` / `bool(tensor)` / data-dependent Python branch inside a
per-eval or per-candidate loop is not a style issue — it is a 100× regression, and the CI
host-sync profiler gate will red your commit. The hot loop lives entirely inside PyTorch;
generation-cadence host code is O(1) statements per generation, never per candidate, never
per eval. If your design needs a per-eval host decision, the design is wrong — restate it as a
batched tensor op or return BLOCKED.

## THE PACK-ERA LAWS (owner, 2026-08-03 — q-089…q-094; binding on every pack/eval/search dispatch)

1. **No ceremony, no intra-process crypto (q-090).** The engine never re-proves anything to
   itself at run time: no digests, identity tokens, version tokens, receipt validation, or
   hashing anywhere on the generation/wave/eval path. Trust model = single producer, monolithic
   process, hard-fail loudly and reboot. A static guard bans reintroduction. (Boundary artifacts
   that leave the process — filing/export provenance — are a different class and stay.)
2. **Dedupe keys are raw sparse records, never hashes (q-091).** Tabu/completion identity =
   the canonical record limbs themselves (member-validity bitset + section words; placement =
   bit-packed slot_active). Key completeness is the acceptance bar — aliasing two physically
   distinct designs is the false-completion bug class.
3. **Resident pool, upload-and-edit (q-092).** Device pool tensors are allocated once and
   EDITED: novel-only slice uploads, survivor-compacted downloads. Re-shipping a whole pack
   per wave and re-deriving per-structure tables per wave (recovery) are named defect classes —
   per-structure tables are built once at pack time and cached in the pool.
4. **Nonblocking station threads (q-093).** Pack runs as a CPU worker pool overlapping eval;
   ONE MPS-owner thread submits all GPU work; results must be independent of thread schedule
   (deterministic merge / order-independent accepts); a dead thread hard-fails the run. Queue
   handoffs carry tensors + ordinals only. Width is a ceiling, never a quorum (b-919).
5. **Sparse/banded pack factor (q-089)** — RCM-reordered band kernel with computed fail-closed
   dense fallback; G stays exact f64; never add a dense-blind n³ path.
6. **Operate at the measured knee (q-094).** Width changes cite a production-path sweep receipt
   in `docs/parallelism-width-doctrine-v1.md` §1/§1c — never a synthetic bench.

## The one thing you must not lose

Every Megapack meets the structure at **12 hardpoints** (per-hardpoint weight from the cited
`hp_load_frac` table, never an uncited uniform split). Every pound flows: hardpoints → rails
(dunnage beams under the pack) → girders/posts → bearing nodes on the RC bond beam capping the
masonry walls → walls → foundations → soil. Dunnage never bears on the roof deck. **A load with
no complete connected path to bearing is not a bad candidate — it is not a structure, and the
engine must refuse to score it.** This brief covers the gravity/static path; wind/seismic/lateral
have their own registry bits and specs.

## The arbiter

`specs/placement-physics-north-star-v1.md` is ground truth for this subsystem. For any
batch/launch WIDTH or throughput question, `docs/parallelism-width-doctrine-v1.md` is the
canonical measured guidance (per-stage widths, knees, live holdups) — do not re-derive widths. Implement its
pseudocode block-for-block. **If your code disagrees with the pseudocode, your code is wrong.**
If a block cannot be implemented as written, STOP and report the conflict in your RESULT — do not
reinterpret it. The decision-lock (`specs/decision-lock-2026-06-21.md`) wins above everything.

## Forbidden moves — any one of these fails your review outright

| # | Forbidden | Law |
|---|---|---|
| 1 | Python loop / per-candidate host sync / batch-size-1 tensor calls in a loop / data-dependent branch / functools cache in a HOT-tier function (this is a **MHz** loop; one procedural leak = 1000× regression) | D-Q77, North Star §7 |
| 2 | Free (x,y) placement; any placement DOF not rail-relative (slots derive FROM rails) | D-Q44 |
| 3 | Identity or opaque `S`; `S` not derived from slot×rail geometry; renormalizing a deficient S-row to hide off-rail mass | North Star §6.1 |
| 4 | Member endpoint that is not a node; plan-crossing without a shared MEMBER_JOINT | North Star §5.2/§5.3 |
| 5 | Raising `MAX_STRUCT_CONFIGS`; churn the pool in ≤512 chunks instead | D-Q43/45 |
| 6 | Widening any tolerance/delta band without a measurement | validation ladder |
| 7 | Silent fallback/skip; bare `assert` for an engine invariant (dies under `-O`); downgrading an engine-error check to a warning | mandate |
| 8 | Wall/soil demand from anything but `G_wall = M @ G_R` rows (seg-len-weighted balance) | North Star §5.4 |
| 9 | Scalar fallback beside a tensor op — tensor-only or stop and report | keystone |
| 10 | Filing-grade claims beyond scope; uncited constants | SC-51 |
| 11 | Editing the North Star, decision-lock, REQUIREMENTS.md, or any guard-test tolerance to make your code pass — spec changes are controller/user-only | North Star §9.11 |
| 12 | NARROWING THE PRODUCTION INNER EVAL WIDTH below the pinned value in `core/search/search_config.py` (`INNER_SWEEP_WIDTH_MVP`, currently 131_072; the hard floor is ≥10,000). PyTorch eval throughput is TERRIBLE at narrow widths — the n=64/inner_width=8 values in tests, benches, and `prototype/engine/` are wiring-only and NOT indicative. Copying a test width into production is the exact regression this row exists to stop; it has happened repeatedly (incl. a stale 10_000 in THIS row masking a 13× narrowing — read the constant, don't trust prose). | D-Q13 |
| 13 | ANY WORK THAT SCALES WITH CANDIDATE COUNT IS HOT-TIER, REGARDLESS OF WHICH FILE OR TIER LABEL IT SITS IN. A `WARM-EVAL` header licenses per-GENERATION bounded work ONLY — never a Python loop, hash, list-build, or numpy round-trip over the candidate batch. THE TIER LABEL DOES NOT EXEMPT YOU: per-candidate Python anywhere between mutate and archive insert is the MHz-killer. This class has recurred 3× (per-eval CSR digest #62; 2026-06-30 PROCEDURAL-CREEP verdict; 2026-07-13 b-244: 160k blake2b/gen in `_f32_archive_preview_masks` → whole engine pinned at 50k evals/s while the kernel does 12M/s). If your change adds work per candidate outside the tensor kernel, it is wrong — restructure so wide paths are tensor-only and per-candidate work runs only on gate survivors. | D-Q77, b-244 |

## Error taxonomy (get this right or the fix is wrong)

- **ENGINE ERROR** = the engine's own math is inconsistent (conservation identity fails, S row ≠ 1)
  → raise loudly via un-strippable check helpers. Never a status, never a bare `assert`.
- **INVALID CONFIG** = generator proposed a non-structure (floating member, unmated crossing)
  → fail-closed fail bit, counted per reason, excluded from archive, id remembered so it is never
  re-proposed.
- **INFEASIBLE** = real structure that fails physics → valid, honest result with the dominant bit.

## Your acceptance criteria (all of them, no exceptions)

1. The adversarial guard test for your change fails RED **against the pre-change production code
   path** (not a synthetic helper) and GREEN after. Cases: pack-off-rail ⇒ `UNSUPPORTED_HARDPOINT`;
   orphan member ⇒ `FLOATING_BEAM`; mid-span-only connected member passes R4; crossing without
   node ⇒ clash; equilibrium identity (seg-len-weighted, seeded k≥64 random P) holds.
2. Deterministic replay: same seed ⇒ bitwise-identical placement generation tensors; identical
   winner set and fail-bit population on the same device (R11 / North Star §8 T-C).
3. Perf floors green: `pytest tests/perf/test_engine_throughput_floor.py` (pack ≥100 kHz) and the
   serial perf phase of the two-phase full gate; no new host↔device sync inside the inner loop.
4. f32/f64 parity semantics untouched: safety direction (no false-PASS) is sacred; winner-RANK
   flips between f32/f64 are FINE — do not "fix" ranking parity.
5. Any S/geometry cache is keyed by `(structural_config_id, slot_table_digest)` — prove reuse
   across configs is impossible.
6. Verify from INSIDE your worktree with relative pytest paths (shared editable install hazard).

## Review lens (for reviewer dispatches)

Check the diff against North Star §4–§7 block-by-block. A placement-generation change that does
not touch S construction (§6.1) is presumptively wrong. A "perf fix" that adds a list, dict,
`for`-over-candidates, or a batch-size-1 tensor call in HOT tier is the regression class we are
hunting, not a fix. Confirm the RED test ran against pre-change production code, and that no spec
or tolerance was edited to make the diff pass.

## Anti-procedural regression protocol (OWNER RULE 2026-07-13 — binding for every hot/warm patch)

1. **In-loop self-check (you, the worker, every internal iteration):** before each self-review
   pass, re-scan YOUR OWN diff for the procedural classes: per-candidate/per-batch Python
   (`for`-over-candidates, list/dict builds, `.tolist()`, `.item()`, host↔device sync) in HOT or
   WARM tier; per-batch recomputation of config-level constants (bake at pack/prep instead);
   batch-size-1 tensor calls. Add/extend a call-counter or width-scaling assertion for any code
   you touched on those paths — prose comments alone do not count as prevention.
   **The guard must COUNT THE RIGHT THING (recurred 3× — F/G/FR):** count HOST SYNCS
   (`.cpu()`/`.item()`/`.tolist()`/`.numpy()`) and TENSOR LAUNCHES, NOT API-entry calls — a
   guard that counts entry calls PASSES while the prohibited per-candidate sync leaks. Include a
   BITE test: temporarily inject a per-candidate sync / `range(L)` loop and prove the guard FAILS;
   the differential form (two different universe sizes → identical op-count/shapes at fixed L) is
   the strongest. A guard that is green but cannot be made to fail on injected creep does not bind.
2. **Controller-side lens on presentation:** every hot/warm patch gets ≥1 dedicated
   anti-procedural reviewer dispatch upon COMPLETE, before acceptance — this is in ADDITION to
   the general review. Expect it; leave your patch reviewable (clean commit series, receipts in
   the final message).
3. **Kill-and-profile after 1 minute:** any engine invocation you expect to be fast that exceeds
   ~1 minute is NOT waited on — kill it and re-run under cProfile (or attach py-spy) to find the
   leak. Never "let it grind to see"; wall-clock surprises are evidence, and the profile is how
   they testify. Report the top-cumtime rows with your receipts.
4. Width context for any timing evidence: state the width it was measured at and whether that is
   a doctrine operating point (`docs/parallelism-width-doctrine-v1.md` §1). Sub-doctrine widths
   localize, they do not quantify.

## MECHANICS LAWS (owner-ratified 2026-07-20 — bans with teeth; each recurred before being banned)

M1. NO process spawning inside pack/eval/search paths. Parallelism = batched tensors IN-PROCESS
    (one stacked BLAS call; Accelerate uses the cores internally). macOS multiprocessing is
    spawn — every worker re-imports torch (~2s) and oversubscribes BLAS threads; a spawn-per-batch
    pool once turned ~3s of factor work into 28s of wall-clock. If a pool is truly unavoidable
    (it is not, for packing): created ONCE per run, warm, OMP/VECLIB threads pinned to 1,
    shared-memory arrays. A static guard bans multiprocessing/ProcessPoolExecutor in core/*.
M2. Python work never scales with candidates, members, families, or stations (D-Q77 as a SCALING
    LAW, not advice — it recurred 3x as prose). A python loop whose trip count grows with the
    design is a defect even when tests pass; vectorize (group-by-shape + einsum/bmm/gather) or stop
    and say so. Guards: call counters + width-scaling tests where present; add one with your change.
M3. except: TYPED ONLY in scoring/pack paths — engine/numeric errors PROPAGATE
    (tests/static/test_search_exception_laundering_guard.py enforces; allowlist additions need a
    written justification in the entry).
M4. REUSE the canonical helper — minting a parallel one is a defect (the parallel util probe
    contradicted fail-bit semantics; the hand-assembled card broke the gallery). Before writing a
    helper: grep for the canonical one (utilization probe = pack_executor's; notices = the one
    formatter; card/artifact emission = the production projectors, NEVER hand-built dicts).
M5. Any perf claim ships with its OVERHEAD DECOMPOSITION receipt: spawn/import time, BLAS thread
    config, copy/serialization share, guard share, residual — alongside the rate. A wall-clock that
    defies flops/bandwidth arithmetic is reported as a finding, not shipped as a result.


## BANDWIDTH LAW (owner directive 2026-07-27)

Memory traffic is the scarce resource in this solver, not FLOPs — the MHz eval loop and the pack
path are bandwidth-bound on Apple Silicon. Every hot- or pack-path change accounts BYTES-TOUCHED
per candidate (reads + writes, including scratch) before/after the change; no new per-candidate
tensor materialization; no dtype up-casts in the hot loop; no scratch that scales with P*dof^2
without a written budget against the tensor-memory projector ceiling
(core/tensor_memory_config.py — the 16->128 resident raise projected 351 GB and was reverted).
"It fits in RAM" is the floor, traffic is the cost. Perf claims name their width and bytes/candidate.

# TASK b-2759 (P2) — DIAGNOSE-THEN-FIX: rail crossing called ORPHAN at 0.0057 inch from its top chord

## The measured failure (real building 4013060125, live rotation sweep, tip 7b6be9992)
BLANKET_PHYSICAL_RAIL_CROSSING_ORPHAN raised for a crossing 0.000476 ft (= 0.0057 inch) from
its nearest top chord — physically ON the chord. Raise sites: core/search/blanket_physical_rails.py
(:529 opposite=no_top_chord, :682, :772 at this tip). The signature suggests an exact
comparison (or a tolerance far tighter than fabrication/geometry reality) where a snap/derived
coordinate carries float error.

## Method (diagnose FIRST, then fix; both in one dispatch)
1. Reproduce the classification path: find how the crossing/top-chord coordinates are derived
   (snapped spans? projected grids?) and WHERE the 0.000476 ft discrepancy is born — is one
   side derived via a different arithmetic route than the other (classic drift: ft vs mft
   round-trips, projection then re-derivation)? Cite file:line for the divergence.
2. THE FIX MUST BE PRINCIPLED, NOT A MAGIC EPSILON: prefer (a) making both sides derive the
   coordinate through the SAME canonical route (no tolerance needed — exact equality of
   identical arithmetic), which is the North-Star way (pseudocode wins; determinism §8); only
   if a genuine cross-route comparison is unavoidable, (b) a tolerance with a DERIVATION
   comment: premise (the max float error accumulable through the named routes) → algebra →
   unit check → sanity check (why 0.0057 inch passes and a real gap fails). NEVER a bare
   1e-3-because-it-works.
3. Check the OTHER two raise sites (:529, :772) for the same class — one catch, one class:
   if the same cross-route comparison shape exists there, fix all of them, same principle.
4. RED-first: a test reconstructing the 4013060125 shape (real-building-derived coordinates —
   extract the actual failing pair from the sweep evidence shape, or reproduce via the real
   import if reachable offline; NO invented geometry that merely happens to fail) that is RED
   on current production and GREEN after. Plus a negative test: a genuinely detached crossing
   (a real gap, e.g. half a rail spacing) STILL raises — the guard must not be weakened.

## HARD CONSTRAINTS
- This is core/search hotpath territory: the PLACEMENT-HOTPATH-CONTEXT above binds; North
  Star pseudocode wins over code; no Python iteration over candidates; determinism preserved.
- Do NOT weaken the orphan guard (the negative test proves it still bites).
- Do NOT edit application/packer_product_seed.py.
- Real buildings only in tests.

## Deliverable
Commits on fix/b2759-rail-crossing-tolerance (base 7b6be9992 = current tip). Explicit
pathspecs; do NOT push. RESULT: the divergence mechanism with file:line, which fix shape
(a) or (b) and why, the derivation if (b), all three raise sites' verdicts, RED+negative
test outputs with bare exit codes, anything not established.

```

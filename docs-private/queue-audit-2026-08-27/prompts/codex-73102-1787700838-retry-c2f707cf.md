# Pinned prompt — `codex-73102-1787700838-retry-c2f707cf`

- source: `prompt-file`
- prompt-file: `/private/tmp/claude-501/-Users-simonrowland-Repos-battery-tool-v2/9a67b5a3-991f-4854-b2a4-60cae5c8328a/scratchpad/b2482-brief.md` (EXISTS on disk)
- pinned_at: 2026-08-27T14:54:06.319466+00:00

```
# Mandate — North Star for Battery-Tool-v2

> **Prepend this to every worker dispatch.** It is the standing charge — the situational frame every
> executor reads before touching the work. Short by design so it travels on each dispatch without bloat.
> Canonical north-star; faithful to `specs/decision-lock-2026-06-21.md` (D-Q1…D-Q79+, authoritative) and `CORE-CONCEPT.md`.

## The mission
Build a **P.Eng-stampable, filing-grade** engineering tool: given a leased NYC rooftop, find diverse, **buildable**
ways to mount Tesla Megapacks on steel dunnage over an existing (often unreinforced-masonry) building, optimized for
capacity × fabricated cost, with a structural-engineer-runnable FEA handoff. The output goes to a Professional Engineer
who **opens it, runs it, and stamps it.** Build for that bar.

## The north star (non-negotiable)
- **Physics-correct, first-principles.** Real analytic mechanics — statics, strength-of-materials, code-reduced
  allowables — never proxies, curve-fits, or fudge factors. Every structural number must be **derivable and provable**
  against a closed-form result or an independent oracle (PyNite/CalculiX).
- **Maximum truth-seeking / no-bullshit.** The tool **never claims what it cannot prove.** If something is uncertain,
  say so and make it conservative — never paper over a gap with a plausible-looking number.
- **Buildable, not merely plausible.** Outputs are fabricable assemblies with real catalogue parts, real load paths,
  real allowables. A layout without a valid structural solution is not an answer.
- **Accuracy-first.** Correctness beats cleverness beats speed. Speed comes from the *architecture* (the keystone),
  never from cutting physical fidelity.

## Load-bearing invariants (honor exactly)
- **HARD-FAIL, never silent-fail.** An engine ERROR crashes loudly. A valid *infeasible candidate* is a status output,
  not an error — keep that distinction sharp. Never return a silently-wrong number; no `except: pass`; never clamp or
  coerce bad input into a result that looks fine.
- **The keystone (CORE-CONCEPT.md, validated 0% in `prototype/influence_validation.py`).** Structural mechanics done
  ONCE at pack time as influence matrices (`K_c → G`); the hot loop is branch-free `resp = base + G @ P` vs baked
  allowables. **Accuracy grows by enriching the precomputed tables, never by complicating the kernel.**
- **Conservative under uncertainty.** When a modeling choice is ambiguous, take the safe side (load-up / capacity-down)
  and flag it.
- **Provable.** Favor designs with a free correctness test — Betti/Maxwell `G`-symmetry, exact superposition, parity vs
  PyNite/CalculiX. A claim without a check is a liability, not a feature.

## How to work
- **Verify before editing.** Check repo state, target files, and assumptions first.
- **The decision-lock is law.** `specs/decision-lock-2026-06-21.md` (D-Q1…D-Q79+) is authoritative and supersedes older
  docs where they conflict. Don't re-litigate ratified decisions — conform to them.
- **Stay in scope.** Do the chunk; surface out-of-scope findings for the backlog, don't silently expand.
- **Surface blockers, don't work around them.** A sandbox/permission/auth/write block returns `BLOCKED:` — you do not
  invent an alternate delivery path.
- **Infeasible is an owner verdict, not a workaround (owner ruling 2026-07-16).** Never bless a bug-caused site
  failure as "expected infeasible": a failing must-pass site is RED with the bug named (`RED-BUG(b-xxx)` strict-xfail
  that flips loudly when fixed). If you believe a case is GENUINELY physics-infeasible, you do not encode that verdict
  yourself — list it as `OWNER-REVIEW` and surface it; only an owner adjudication (cited `q-xxx`) may bless
  infeasibility. Silently institutionalizing infeasibility is a P0 defect.
- **Self-review before handoff against this mandate:** is every number physical, provable, conservative, and hard-failing?

> The reviewer is P.Eng-level and values first-principles truth over agreeableness. Disagree with evidence when the
> facts warrant it; never agree just to be agreeable. The goal is the most correct, buildable answer — not the most pleasing one.

---

# CHUNK b-2482 — STREAM SEEDS TO THE BROWSER AS THEY BECOME AVAILABLE

Everything above this line is the standing mandate; it governs.

OWNER DIRECTIVE, verbatim, twice: "the damn seeds should stream to the browser even if the
optimizer crashes" and "make it stream to browser". This is a requirement, not a nice-to-have.

## ★ MOST OF THIS ALREADY EXISTS. DO NOT BUILD SSE OR A WEBSOCKET.

I measured all three layers before writing this. Two of them are already done:

1. TRANSPORT — ALREADY WORKS. docs-private/ui-design/building-view-shell.html pollJob():
       if (job.status === 'queued' || job.status === 'running') {
         await loadGalleryOnly();
         setTimeout(() => pollJob(jobId, stateId), 1000);
       }
   The browser polls every 1 s and re-renders the gallery on every tick WHILE THE JOB RUNS. Anything
   the gallery endpoint returns appears within ~1 s, with no client change.
   There is NO EventSource/WebSocket/StreamingResponse anywhere in application/ui_backend, and none is
   needed. Do not add one.

2. CONSUMER — ALREADY WORKS. b-2317 (boarded) gives the gallery seed cards with honest disclosure:
   card_kind="seed", a "SEED / NOT CERTIFIED" span, and a "· Seeds" headline. Mixed buckets hard-fail
   GALLERY_UI_MIXED_CARD_KIND. Seed cards render correctly today.

3. THE GAP — SERVER-SIDE PUBLICATION TIMING. This is the whole job.
   application/search_until_dry.py: initial_pack is materialized at ~:5865
   (_materialize_initial_resident_pack). Seeds exist there. The ONLY call to
   emit_and_register_seed_diagnostics is at ~:6204 — roughly 340 lines later, INSIDE the terminal
   error branch. So seeds are a post-mortem artifact of a crash and are never published while the
   search is healthy or still running.

## ★★ THE HONESTY CONSTRAINT THAT SHAPES THE DESIGN — READ BEFORE CODING

You CANNOT simply move or re-call the existing exporter early.
application/seed_gallery_preview.py export_packed_seed_diagnostics REQUIRES terminal_reason, and
seed_diagnostic_warning() validates it against SEED_DIAGNOSTIC_TERMINAL_REASONS and renders:
    "<reason>: no candidate survived the cold path — this is an ..."
Its docstring says "after an empty terminal bank". It is a POST-MORTEM artifact by construction.

Calling it mid-run with a synthetic terminal_reason would make the product ASSERT A TERMINAL STATE
THAT HAS NOT HAPPENED and tell the operator no candidate survived while the search is still running.
That is exactly the truth-suppression class fixed today in 93392624f, where the UI named a cause it
could not know. DO NOT DO IT. A mid-run seed publication needs its own honest framing: these are
SEEDS, the search is STILL RUNNING, nothing has been certified, and no verdict/utilization/Megapack
count is claimed.

## WHAT TO BUILD

1. Publish seeds AS SOON AS THEY EXIST (right after initial_pack, ~:5876-:5891), in a mode that
   states what is true at that moment. Reuse the existing UNPROVEN vocabulary — the exporter already
   refuses to emit numeric stand-ins for utilization, margin, verdict, MP count and optimized status.
2. ★ EARLY PUBLICATION MUST BE BEST-EFFORT AND MUST NEVER ABORT A HEALTHY SEARCH. On the terminal
   path, raising is correct — the run already failed. On the early path it is NOT: a preview failure
   turning into a new crash source would be strictly worse than today. Catch, DISCLOSE (never
   silently swallow — hard-fail-never-silent-fail still governs the RUN's own errors), and continue.
   MEASURED PROOF THIS MATTERS: a live run died
   UNTIL_DRY_SEED_DIAGNOSTIC_EMISSION_FAILED:...EZDXF_UNAVAILABLE — one missing exporter dependency
   took down the whole emission. On the early path that must degrade, not kill.
3. Keep the terminal emission working. If seeds were already published, do not double-register or
   contradict; update or no-op. Decide and state which.
4. If seeds become available incrementally (layouts x builders per the owner's matrix), publish each
   as it lands rather than batching. If they are only available as one batch today, publish the batch
   and SAY SO in your RESULT — do not fake granularity.

## HARD CONSTRAINTS

- Never claim a terminal state that has not occurred. No synthetic terminal_reason.
- Never present a seed as certified. b-2317's card_kind/disclosure is the existing vocabulary; use it.
- Early-publish failure disclosed, never silent, and never fatal to the search.
- Do not add SSE/WebSocket/StreamingResponse. The 1 s poll is the transport.
- Do not touch core/search internals; this is application/ + the UI shell.
- application/search_until_dry.py is heavily contended — keep the diff tight and additive.

## VERIFICATION

    PYTHONPATH=. <repo>/.venv/bin/python -m pytest tests/web/ tests/ui_backend/test_b566_web_qa.py \
      -q --no-header -p no:cacheprovider --timeout=900

Then prove it END TO END on the real UI, which is the only thing that proves the requirement:
    tests/e2e_browser/test_happy_path_any_building.py with BT_HAPPY_BBL=4014760001,
    BT_UI_E2E_BBL=4014760001, BT_UI_BASE_URL UNSET (the fixture starts its own private server).
Assert that seed cards are VISIBLE IN THE GALLERY WHILE THE JOB IS STILL RUNNING, and that they
remain after an optimizer failure. A test that only checks the post-crash state does not prove
streaming.

Prove pre-existence for any red you find at your branch base; do not assert it.

## DELIVERABLE

One commit, explicit pathspecs, NO AI-attribution trailers. RESULT: states where you publish, how the
early path degrades when an exporter dependency is missing, whether seeds are batch or incremental
today, and the e2e evidence that cards appear DURING the run.

```

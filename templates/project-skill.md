# <PROJECT_NAME>

This repository is configured for Goal Flight controller workflows.

Read order:

1. `AGENTS.md`
2. this `SKILL.md`
3. the invoked Goal Flight `commands/*.md` file from skill-root

Project verification:

- test: `<command>`
- lint: `<command>`
- build: `<command>`

Keep long-running state, review artifacts, env caveats, queues, and resume notes
under ignored `docs-private/`.

Read the newest docs-private/RESUME-NOTES-*.md and continue <project> per its
"Current goals" / "Next action". Re-ground in the files under "Key pins"
(AGENTS.md is the file map). Respect "Do not re-litigate". If blocked, record the
decision in docs-private/questions-for-user.md (list the task ids it blocks) and
move on.

This prompt names no specific tasks on purpose — so it never goes stale. The
living state is the newest RESUME-NOTES file; keep that current, not this prompt.
Find newest: `ls -1 docs-private/RESUME-NOTES-*.md | sort | tail -1`.

# <project> — Questions for the user (async) + decisions (ADRs)

> Open decisions that block work, surfaced for async answering. Each question
> lists the task chunk ids it BLOCKS. When answered, convert it into an ADR
> heading in "Decided" below — decisions live in THIS file so agents grep one
> place. The dashboard surfaces the open ones (with links to the blocked tasks).
> Grep: `### Q-` for open questions, `### ADR-` for decisions.

## Open — needs a decision

### Q-001

**<the question>**

- Blocks: `t-014`, `t-016`
- Options: <a> / <b>
- Recommendation: <…>

## Decided (ADRs)

### ADR-001 — <decision> (resolves Q-001; unblocked t-014, t-016; `<date>`)

- Decision: <what was chosen>
- Why: <one or two lines>

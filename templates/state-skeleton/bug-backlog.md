# <project> — Bug Backlog (open)

> Open bug INSTANCES. Each heading is the bare bug id (anchor = id, e.g.
> `bug-backlog.md#b-001`). On fix, MOVE the block to `bugs-done.md`. Recurring
> shapes get a class in `bug-patterns.md`.

## b-001

**<title>**

- Pattern: <bug-patterns id, if any>
- Repro / evidence: <…>

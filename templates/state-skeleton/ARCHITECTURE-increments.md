# <project> — Incremental Architecture

> Living, staged plan. The architecture is reached in increments; this file is
> the source of truth for "where we are and what's next". Update as each
> increment lands. Pairs with `ARCHITECTURE.md` (as-built).

## Done (landed increments)

- [x] <increment> — <one-line outcome> (`<date>` / `<SHA>`)

## Next (the increment in flight)

- [ ] <the current increment> — acceptance: <what "done" means>

## Target (where this is heading)

<the end-state architecture in a few lines — the shape we're building toward>

## Open architectural questions

- <question to resolve — raise blocking ones in questions-for-user.md>

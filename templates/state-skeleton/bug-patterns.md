# <project> — Bug Patterns (classes)

> Bug SHAPES (classes / predicates) caught here. Promote cross-project shapes to
> the shared corpus (`~/Repos/.agent-context/sweep-corpus/`) and link the SC-id.
> Instances live in `bug-backlog.md` / `bugs-done.md`.

_(none recorded)_
